# Auth Real + Email Implementation

**Status: In Progress** (usando Ethereal para testes gratuitos)

## Plano:
1. ✅ Add nodemailer & dotenv to package.json  
2. Add IPC + nodemailer to main.js  
3. Replace preload auth mocks → IPC  
4. Update esqueci-senha.js + login.js  
5. .env config  
6. Test npm install && npm start

## Next Steps:
### 1. ✅ [DONE] package.json + npm install (nodemailer, bcryptjs)

### 2. ✅ [DONE] DB auth.js helpers + schema/migrations

### 3. ✅ [DONE] main.js IPC handlers (register/login/sendCode/verify/reset)
